spring:
  application:
    name: user-service
  cloud:
    config:
      discovery:
        service-id: CONFIG-SERVER
        enabled: true
      profile: dev
      label: master

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:100/eureka/
